{
  "name": "intissar-ai",
  "version": "1.0.0",
  "description": "انتصار AI — تطبيق تسجيل وتفريغ صوتي عربي، يعمل offline بالكامل عبر Capacitor + Vosk.",
  "private": true,
  "scripts": {
    "build-android": "npx cap add android && node scripts/bundle-offline-assets.js && node scripts/patch-native-android.js && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "jszip": "3.10.1",
    "mammoth": "1.4.9",
    "pdfjs-dist": "3.11.174",
    "tesseract.js": "5.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
